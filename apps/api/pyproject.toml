[project]
name = "changetools"
version = "0.1.0"
description = "Agentic AI workflow that turns change-programme materials into a structured brief and slide deck."
readme = "README.md"
requires-python = ">=3.11"
authors = [{ name = "Rauf Khan" }]
license = { text = "MIT" }

dependencies = [
    # Web framework
    "fastapi>=0.115",
    "uvicorn[standard]>=0.30",
    "python-multipart>=0.0.9",
    "httpx>=0.27",

    # Config + validation
    "pydantic>=2.7",
    "pydantic-settings>=2.4",

    # Logging
    "structlog>=24.1",

    # LangChain / LangGraph ecosystem
    "langchain>=0.3",
    "langchain-core>=0.3",
    "langchain-community>=0.3",
    "langchain-groq>=0.2",
    "langchain-openai>=0.2",
    "langchain-anthropic>=0.2",
    "langgraph>=0.2",
    "langsmith>=0.1.130",

    # Embeddings
    "sentence-transformers>=3.0",

    # Database
    "sqlalchemy[asyncio]>=2.0",
    "greenlet>=3.0",
    "alembic>=1.13",
    "psycopg[binary]>=3.2",
    "pgvector>=0.3",

    # Document parsing
    "python-docx>=1.1",
    "openpyxl>=3.1",

    # Slide rendering
    "python-pptx>=1.0",
    "Pillow>=10.4",

    # Storage
    "boto3>=1.35",

    # Misc
    "tenacity>=9.0",
    "numpy>=1.26",
]

[dependency-groups]
dev = [
    "pytest>=8.3",
    "pytest-asyncio>=0.24",
    "pytest-cov>=5.0",
    "anyio>=4.4",
    "ruff>=0.6",
    "mypy>=1.11",
    "types-boto3>=1.35",
    "respx>=0.21",
    "freezegun>=1.5",
]

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["src/changetools"]

[tool.ruff]
line-length = 100
target-version = "py311"
src = ["src", "tests"]

[tool.ruff.lint]
select = [
    "E", "F", "W",   # pycodestyle / pyflakes
    "I",             # isort
    "B",             # bugbear
    "UP",            # pyupgrade
    "N",             # pep8-naming
    "SIM",           # simplify
    "RUF",           # ruff-specific
    "T20",           # no print
    "PT",            # pytest style
]
ignore = [
    "E501",   # line length — ruff format handles it
    "B008",   # FastAPI ``Depends(...)`` in defaults is idiomatic
]

[tool.ruff.lint.per-file-ignores]
"tests/**/*.py" = ["S101"]   # asserts allowed in tests
"src/changetools/eval/__main__.py" = ["T201"]   # CLI prints intended

[tool.ruff.format]
quote-style = "double"
indent-style = "space"

[tool.mypy]
python_version = "3.11"
strict = true
warn_unused_ignores = true
warn_return_any = true
warn_unreachable = true
plugins = ["pydantic.mypy"]
files = ["src"]
exclude = ["tests/", "alembic/"]

[[tool.mypy.overrides]]
module = [
    "sentence_transformers.*",
    "pgvector.*",
    "boto3.*",
    "botocore.*",
    "docx.*",
    "openpyxl.*",
    "pptx.*",
]
ignore_missing_imports = true

[tool.pytest.ini_options]
testpaths = ["tests"]
asyncio_mode = "auto"
addopts = "-ra --strict-markers"
markers = [
    "integration: requires a running database",
    "eval: evaluation harness cases",
]
